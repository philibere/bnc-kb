DATABASE_URL=postgresql://kb:kb@localhost:5433/kb
API_KEY_READ=read-key
API_KEY_WRITE=write-key
API_KEY_ADMIN=admin-key
# Embedder for ingestion AND /search (must match). fake-v1 = deterministic, no torch.
# Real model: EMBEDDING_MODEL=BAAI/bge-m3 + `pip install -e ".[embed]"`.
EMBEDDING_MODEL=fake-v1
# Optional: override the vendored metamodel manifest path.
# BNC_METAMODEL=/path/to/bnc.metamodel.yaml

services:
  db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_USER: kb
      POSTGRES_PASSWORD: kb
      POSTGRES_DB: kb
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kb"]
      interval: 2s
      timeout: 3s
      retries: 20
